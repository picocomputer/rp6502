name: CI

on:
  push:
    branches: [main]
    tags: ['v[0-9]*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

env:
  PICO_SDK_VERSION: "2.3.0"
  PICO_TOOLS_TAG: "v2.3.0-0"
  ARM_TOOLCHAIN: "15.2.Rel1"

jobs:
  # ---------------------------------------------------------------------------
  # version: the one place the tag is parsed. Every job that stamps a binary or
  # names a release asset reads these outputs, so a build and the asset holding
  # it can never disagree about what version they are.
  # ---------------------------------------------------------------------------
  version:
    runs-on: ubuntu-24.04
    outputs:
      version: ${{ steps.v.outputs.version }}
      fwver: ${{ steps.v.outputs.fwver }}
      fwci: ${{ steps.v.outputs.fwci }}
      stamp: ${{ steps.v.outputs.stamp }}
    steps:
      - name: Derive version
        id: v
        run: |
          set -euo pipefail
          # version names files, fwver/fwci go to CMake, and stamp is the same
          # string the build writes into itself — for metadata fields that are
          # filled in by hand out here rather than by a compiler.
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
            echo "fwver=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
            echo "fwci=" >> "$GITHUB_OUTPUT"
            echo "stamp=Version ${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            # Off a tag there is no version, and inventing one that looks like
            # a version is worse than not having it. The run id is what the
            # firmware banner already says it is, and it names a build you can
            # go and look at.
            echo "version=${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"
            echo "fwver=" >> "$GITHUB_OUTPUT"
            echo "fwci=${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"
            echo "stamp=CI ${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"
          fi
          cat "$GITHUB_OUTPUT"

  # ---------------------------------------------------------------------------
  # tools: every project fetches tools/ against the hashes in its manifest, so
  # a tool edited without regenerating it breaks the update for everyone. The
  # person who would otherwise find out is a user, not the author.
  # ---------------------------------------------------------------------------
  tools:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - name: Verify tools/manifest.txt
        run: |
          set -euo pipefail
          cd tools
          grep -E '^[0-9a-f]+ ' manifest.txt | sha256sum -c -
          # A tool left out of the manifest hashes fine and never ships.
          diff <(ls | grep -v '^manifest\.txt$' | sort) \
               <(awk '/^[0-9a-f]+ /{print $2}' manifest.txt | sort)

  # ---------------------------------------------------------------------------
  # pico: a pico2_w build produces rp6502-ria-w.uf2 + rp6502-vga.uf2 and a
  # pico2 build produces rp6502-ria.uf2. All three ship.
  # ---------------------------------------------------------------------------
  pico:
    needs: [version]
    runs-on: ubuntu-24.04
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-pico.zip
    steps:
      - uses: actions/checkout@v7


      - name: Install host packages
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build python3 git ca-certificates \
            libusb-1.0-0-dev pkg-config

      - name: Install Arm GNU toolchain (${{ env.ARM_TOOLCHAIN }})
        uses: carlosperate/arm-none-eabi-gcc-action@v1
        with:
          release: ${{ env.ARM_TOOLCHAIN }}

      - name: Cache Pico SDK + picotool + pioasm
        id: pico
        uses: actions/cache@v6
        with:
          path: ~/.pico-sdk
          key: pico-${{ env.PICO_SDK_VERSION }}-cyw43-lwip-btstack-mbedtls-x86_64-v1

      - name: Fetch Pico SDK ${{ env.PICO_SDK_VERSION }} + prebuilt tools
        if: steps.pico.outputs.cache-hit != 'true'
        run: |
          set -euxo pipefail
          SDK_DIR="$HOME/.pico-sdk/sdk/${PICO_SDK_VERSION}"
          git clone --branch "${PICO_SDK_VERSION}" --depth 1 \
            https://github.com/raspberrypi/pico-sdk "$SDK_DIR"
          git -C "$SDK_DIR" submodule update --init --depth 1 \
            lib/cyw43-driver lib/lwip lib/btstack lib/mbedtls
          # Prebuilt picotool (RP2350 .uf2 generation) and pioasm (PIO headers),
          # laid out exactly like the Pico VS Code extension's ~/.pico-sdk.
          mkdir -p "$HOME/.pico-sdk/picotool/${PICO_SDK_VERSION}" \
                   "$HOME/.pico-sdk/tools/${PICO_SDK_VERSION}"
          base="https://github.com/raspberrypi/pico-sdk-tools/releases/download/${PICO_TOOLS_TAG}"
          curl -fSL -o /tmp/picotool.tar.gz "$base/picotool-${PICO_SDK_VERSION}-x86_64-lin.tar.gz"
          tar -xzf /tmp/picotool.tar.gz -C "$HOME/.pico-sdk/picotool/${PICO_SDK_VERSION}"
          curl -fSL -o /tmp/pico-sdk-tools.tar.gz "$base/pico-sdk-tools-${PICO_SDK_VERSION}-x86_64-lin.tar.gz"
          tar -xzf /tmp/pico-sdk-tools.tar.gz -C "$HOME/.pico-sdk/tools/${PICO_SDK_VERSION}"

      - name: Build firmware (pico2_w) — RIA-W + VGA
        run: |
          set -euxo pipefail
          export PICO_SDK_PATH="$HOME/.pico-sdk/sdk/${PICO_SDK_VERSION}"
          cmake -S . -B build -G Ninja \
            -DRP6502_STRICT_SUBMODULES=ON \
            -DCMAKE_BUILD_TYPE=Release \
            -DPICO_BOARD=pico2_w \
            -DRP6502_VERSION="${{ needs.version.outputs.fwver }}" \
            -DRP6502_CI="${{ needs.version.outputs.fwci }}" \
            -DPICO_SDK_PATH="$PICO_SDK_PATH" \
            -Dpicotool_DIR="$HOME/.pico-sdk/picotool/${PICO_SDK_VERSION}/picotool" \
            -Dpioasm_DIR="$HOME/.pico-sdk/tools/${PICO_SDK_VERSION}/pioasm"
          cmake --build build

      - name: Build firmware (pico2) — RIA
        run: |
          set -euxo pipefail
          export PICO_SDK_PATH="$HOME/.pico-sdk/sdk/${PICO_SDK_VERSION}"
          cmake -S . -B build/pico2 -G Ninja \
            -DRP6502_STRICT_SUBMODULES=ON \
            -DCMAKE_BUILD_TYPE=Release \
            -DPICO_BOARD=pico2 \
            -DRP6502_VERSION="${{ needs.version.outputs.fwver }}" \
            -DRP6502_CI="${{ needs.version.outputs.fwci }}" \
            -DPICO_SDK_PATH="$PICO_SDK_PATH" \
            -Dpicotool_DIR="$HOME/.pico-sdk/picotool/${PICO_SDK_VERSION}/picotool" \
            -Dpioasm_DIR="$HOME/.pico-sdk/tools/${PICO_SDK_VERSION}/pioasm"
          cmake --build build/pico2

      # The .uf2 carry the version too, not just the archive: a name you type
      # at a FLASH prompt should say which build it is.
      - name: Package pico
        env:
          V: ${{ needs.version.outputs.version }}
        run: |
          set -euxo pipefail
          mkdir stage
          cp build/ria/rp6502-ria-w.uf2     "stage/rp6502-${V}-ria-w.uf2"
          cp build/pico2/ria/rp6502-ria.uf2 "stage/rp6502-${V}-ria.uf2"
          cp build/vga/rp6502-vga.uf2       "stage/rp6502-${V}-vga.uf2"
          # The README names the files it tells you to FLASH, so the version
          # has to reach it too.
          sed "s/@VERSION@/${V}/g" src/dist/pico/README.txt > stage/README.txt
          ( cd stage && zip -r "$GITHUB_WORKSPACE/${ARCHIVE}" . )

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # linux: build the windowed emu, run the CTest suite, ship a tarball per
  # arch. The binary inside is plain rp6502-emu — the arch is the archive's
  # business, not the program's.
  # ---------------------------------------------------------------------------
  linux:
    name: linux-${{ matrix.arch }}
    needs: [version]
    strategy:
      fail-fast: false
      matrix:
        include:
          - { runner: ubuntu-22.04,     arch: x86_64 }
          - { runner: ubuntu-22.04-arm, arch: aarch64 }
    runs-on: ${{ matrix.runner }}
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-linux-${{ matrix.arch }}.tar.gz
    steps:
      - uses: actions/checkout@v7


      - name: Install build + windowed-emu dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build \
            libgl-dev libx11-dev libxi-dev libxcursor-dev libasound2-dev

      - name: Configure & build emulator (Release)
        run: |
          set -euxo pipefail
          cmake -S src/emu -B build/emulator/release -G Ninja \
            -DRP6502_STRICT_SUBMODULES=ON -DCMAKE_BUILD_TYPE=Release \
            -DRP6502_VERSION="${{ needs.version.outputs.fwver }}" \
            -DRP6502_CI="${{ needs.version.outputs.fwci }}"
          cmake --build build/emulator/release

      - name: Run emulator tests
        run: ctest --test-dir build/emulator/release --output-on-failure --parallel $(nproc)

      # tar, not zip: it is what a Linux user expects and the only container
      # here that carries the execute bit through GitHub's artifact round-trip.
      - name: Package linux-${{ matrix.arch }}
        run: |
          set -euxo pipefail
          mkdir stage
          cp build/emulator/release/rp6502-emu stage/
          cp src/dist/linux/README.txt stage/
          tar -czf "$ARCHIVE" -C stage rp6502-emu README.txt

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # macos: build and test on arm64, wrap the binary in an unsigned .app.
  # ---------------------------------------------------------------------------
  macos:
    needs: [version]
    runs-on: macos-15
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-macos.zip
    steps:
      - uses: actions/checkout@v7


      - name: Configure & build emulator (Release, arm64)
        run: |
          set -euxo pipefail
          cmake -S src/emu -B build/emulator/release -G Ninja \
            -DRP6502_STRICT_SUBMODULES=ON \
            -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DRP6502_VERSION="${{ needs.version.outputs.fwver }}" \
            -DRP6502_CI="${{ needs.version.outputs.fwci }}"
          cmake --build build/emulator/release

      - name: Run emulator tests
        run: ctest --test-dir build/emulator/release --output-on-failure --parallel $(sysctl -n hw.ncpu)

      - name: Package macos
        env:
          FWVER: ${{ needs.version.outputs.fwver }}
          STAMP: ${{ needs.version.outputs.stamp }}
        run: |
          set -euxo pipefail
          # CFBundleVersion wants a dotted number, which a run id is not, so an
          # untagged build says 0.0 rather than lying in Finder's Get Info.
          # CFBundleGetInfoString is free-form and says what the build really is.
          VER="${FWVER:-0.0}"
          mkdir stage
          APP=stage/rp6502-emu.app
          mkdir -p "$APP/Contents/MacOS"
          cp build/emulator/release/rp6502-emu "$APP/Contents/MacOS/rp6502-emu"
          chmod +x "$APP/Contents/MacOS/rp6502-emu"
          cat > "$APP/Contents/Info.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>            <string>rp6502-emu</string>
              <key>CFBundleIdentifier</key>            <string>com.picocomputer.rp6502-emu</string>
              <key>CFBundleName</key>                  <string>rp6502-emu</string>
              <key>CFBundleDisplayName</key>           <string>RP6502 Emulator</string>
              <key>CFBundlePackageType</key>           <string>APPL</string>
              <key>CFBundleInfoDictionaryVersion</key> <string>6.0</string>
              <key>CFBundleShortVersionString</key>    <string>${VER}</string>
              <key>CFBundleVersion</key>               <string>${VER}</string>
              <key>CFBundleGetInfoString</key>         <string>${STAMP}</string>
              <key>LSMinimumSystemVersion</key>        <string>11.0</string>
              <key>NSHighResolutionCapable</key>       <true/>
          </dict>
          </plist>
          PLIST
          cp src/dist/macos/README.txt stage/
          # ditto is the platform's own archiver: it keeps the bundle's mode
          # bits and extended attributes, which zip and upload-artifact do not.
          # No --keepParent, so stage/'s contents land at the zip root.
          ditto -c -k --sequesterRsrc stage "$ARCHIVE"

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # itch.io: build the emsdk web target, ship the bundle it assembles.
  # ---------------------------------------------------------------------------
  itch-io:
    name: itch.io
    needs: [version]
    runs-on: ubuntu-24.04
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-itch.io.zip
    steps:
      - uses: actions/checkout@v7

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential cmake ninja-build python3

      # No emsdk step: the wasm preset's toolchain file fetches the submodule
      # and runs install and activate itself, which is the path a developer
      # takes and the only one that was never exercised here. Nothing is
      # sourced either — the toolchain includes Emscripten's own cmake file,
      # so emcc is found by absolute path rather than off PATH.
      - name: Build web bundle
        run: |
          set -exo pipefail
          cd src/emu
          cmake --preset wasm -DRP6502_STRICT_SUBMODULES=ON \
            -DRP6502_VERSION="${{ needs.version.outputs.fwver }}" \
            -DRP6502_CI="${{ needs.version.outputs.fwci }}"
          cmake --build --preset wasm

      - name: Package itch.io
        run: |
          set -euxo pipefail
          cd build/web/bundle
          zip -r "$GITHUB_WORKSPACE/$ARCHIVE" .

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # windows: build the D3D11 emu with MSVC + Ninja, run the CTest suite,
  # ship rp6502-emu.exe.
  # ---------------------------------------------------------------------------
  windows:
    needs: [version]
    runs-on: windows-2025
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-windows.zip
    steps:
      - uses: actions/checkout@v7

      # Set up the MSVC x64 toolchain via the runner's own Visual Studio (no
      # third-party action), then build in the same step so the dev-shell env
      # (cl.exe, INCLUDE/LIB) is in scope. ctest below only runs the built exes.
      - name: Configure & build emulator (Release)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vsPath = & $vswhere -latest -property installationPath
          Import-Module (Join-Path $vsPath 'Common7\Tools\Microsoft.VisualStudio.DevShell.dll')
          Enter-VsDevShell -VsInstallPath $vsPath -SkipAutomaticLocation -DevCmdArguments '-arch=x64 -no_logo'
          cd src/emu
          cmake --preset release -DRP6502_STRICT_SUBMODULES=ON `
            "-DRP6502_VERSION=${{ needs.version.outputs.fwver }}" `
            "-DRP6502_CI=${{ needs.version.outputs.fwci }}"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          cmake --build --preset release
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # pwsh is the default here, but this file mixes shells, and the core
      # count is spelled a different way in each of them.
      - name: Run emulator tests
        shell: pwsh
        run: ctest --test-dir build/emulator/release --output-on-failure --parallel $env:NUMBER_OF_PROCESSORS

      # Compress-Archive rather than bash zip: Windows ships it, and there are
      # no mode bits here worth preserving.
      - name: Package windows
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory stage | Out-Null
          Copy-Item build/emulator/release/rp6502-emu.exe stage/
          Copy-Item src/dist/windows/README.txt stage/
          Compress-Archive -Path stage/* -DestinationPath "$env:ARCHIVE"

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # android: build the optimized arm64-v8a APK and ship it as it is.
  # ---------------------------------------------------------------------------
  android:
    needs: [version]
    runs-on: ubuntu-24.04
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-android.apk
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      # versionCode must be an integer, so a tag becomes one (0.31 -> 3100) and
      # everything else stays 1. Nothing upgrades this APK in place.
      - name: Build APK (Release)
        env:
          FWVER: ${{ needs.version.outputs.fwver }}
          FWCI: ${{ needs.version.outputs.fwci }}
        run: |
          set -euxo pipefail
          CODE=1
          if [ -n "$FWVER" ]; then
            CODE=$(echo "$FWVER" | awk -F. '{printf "%d", $1*10000 + $2*100 + $3}')
          fi
          cd src/host/android
          ./gradlew assembleRelease \
            -PrpVersion="${FWVER:-}" -PrpCi="${FWCI:-}" -PrpVersionCode="$CODE"

      # An APK is already a zip and Android installs it directly, so it ships
      # bare. Its install warning lives in the release body, not a README.
      - name: Package android
        run: |
          set -euxo pipefail
          cp build/android/gradle/outputs/apk/release/rp6502-emu-release.apk \
            "$ARCHIVE"

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # rtl-tests: verilate the machine and run it against the emu_core oracle.
  # Needs Verilator 5 (ubuntu-24.04 carries 5.020); the verilate() call compiles
  # with -Wall, so any RTL warning fails the build.
  #
  # Long, and none of it is a fit: it is a compiler building a verilated model.
  # So the compile is cached and the suite is not — a gate would have to skip
  # tests to save anything, and a skipped test leaves no verdict where a
  # restored bitstream still leaves a bitstream. There was one, on whether
  # src/rtl changed, and it was wrong in both directions: the benches are in
  # tests/ and the oracle is emu_core out of src/emu, so a commit touching only
  # those skipped the suite that covers it. ccache is what pays for dropping it.
  # ---------------------------------------------------------------------------
  rtl-tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - name: Install build dependencies + Verilator + RISC-V toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build verilator \
            gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf

      # Verilator regenerates its C++ on every configure, but unchanged RTL
      # regenerates identical C++, so the model's compile is a hit.
      - uses: hendrikmuhs/ccache-action@v1
        with:
          key: rtl-tests

      - name: Configure & build simulation (Release)
        run: |
          set -euxo pipefail
          cmake --preset verilator/Release -DRP6502_STRICT_SUBMODULES=ON \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build --preset Tests
        working-directory: src/rtl

      - name: Run simulation tests
        run: ctest --test-dir build/verilator/release --output-on-failure --parallel $(nproc)

  # pocket: the bitstream, and whether it closed. Quartus writes one out either
  # way, so the slack is the thing being tested here — the .rbf_r is a
  # by-product.
  #
  # A fit is tens of minutes and needs a 20 GB toolchain, and this used to run
  # on whether a diff touched src/rtl. That was the wrong question twice over.
  # Yes for the firmware under src/rtl/sw, which costs an assemble and not a
  # placement, and yes for the whole life of a pull request once any commit in
  # it moved the RTL. No for src/ria and src/vga, compiled into the image that
  # assemble installs, and for src/vga/term, which generates the font and
  # palette ROMs in the fabric.
  #
  # So it asks the build graph instead: pocket.cmake writes out the lists its
  # own DEPENDS are made of and each rule gets a cache keyed on exactly those.
  # No condition is left, and the cache is the better gate — this runs every
  # time, so the core in the artifacts is always the core this tree describes.
  pocket:
    needs: [version]
    runs-on: ubuntu-24.04
    env:
      QUARTUS_IMAGE: raetro/quartus:21.1
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-pocket.zip
    steps:
      - uses: actions/checkout@v7

      # The machine's own two, and the two the generated packages come out
      # of: assets.cmake writes w65c02's decode table and the OPL2's lookup
      # ROM at configure time, and it reads chips/codegen and opl2_fpga to
      # do it. This job needs no simulator and no test fixtures, which is
      # why it is not simply test-rtl's list.
      - name: Install build dependencies + RISC-V toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build python3 \
            gcc-riscv64-unknown-elf picolibc-riscv64-unknown-elf

      # Quartus is the only thing here that needs the old image, so it is the
      # only thing that goes inside it. The image is Ubuntu 16.04 and every
      # tag of it was built in one week of July 2022: its glibc is older than
      # any Node the checkout action runs on, and its archives never carried a
      # RISC-V compiler. Running the job in the container fails on the first
      # of those before it can fail on the second.
      #
      # So the container stops being the environment and becomes six programs.
      # Each shim is a docker run over the same workspace mounted at the same
      # path, which is what lets the absolute paths in the generated .qsf mean
      # the same thing on both sides of the mount. find_program() in
      # quartus.cmake takes them off PATH like any other binary — its HINTS
      # name a local install that a runner does not have.
      #
      # --user keeps what Quartus writes owned by the runner, so the host side
      # of the build can still write into the directories it created.
      #
      # No pull here. A shim is found by find_program() whether or not the image
      # exists, which is what lets the tree configure — and so name what a fit
      # would be made from — before anything decides whether one is owed.
      - name: Quartus by way of the container
        run: |
          set -euxo pipefail
          shims="$HOME/.quartus-shims"
          mkdir -p "$shims"
          for tool in quartus_map quartus_fit quartus_sta \
                      quartus_asm quartus_drc quartus_cdb; do
            printf '%s\n' \
              '#!/usr/bin/env bash' \
              'exec docker run --rm \' \
              '  --user "$(id -u):$(id -g)" -e HOME=/tmp \' \
              "  -v \"$GITHUB_WORKSPACE:$GITHUB_WORKSPACE\" -w \"\$PWD\" \\" \
              "  \"$QUARTUS_IMAGE\" $tool \"\$@\"" \
              > "$shims/$tool"
            chmod +x "$shims/$tool"
          done
          echo "$shims" >> "$GITHUB_PATH"

      # One preset. The pocket tree is Quartus and the RISC-V compiler and
      # nothing else — no Verilator, which this runner does not install and
      # which a bitstream never needed. Configuring also writes the input
      # lists, so it is its own step ahead of the keys.
      - name: Configure the pocket tree
        run: |
          set -euxo pipefail
          cmake --preset pocket \
            -DRP6502_VERSION="${{ needs.version.outputs.fwver }}" \
            -DRP6502_CI="${{ needs.version.outputs.fwci }}"
        working-directory: src/rtl

      # Three keys, nested, one per rule in pocket.cmake. Nesting reduces the
      # container to one question: a bitstream hit implies the other two, so a
      # restorable core.bin means no Quartus stage can be owed.
      #
      # The path is inside each file's hash, so a rename counts. The image tag
      # is in there because a different Quartus is a different fit.
      - name: Derive the cache keys
        id: keys
        run: |
          set -euo pipefail
          B=build/pocket/bitstream
          hash_list() { sort "$1" | xargs -r sha256sum | sha256sum | cut -d' ' -f1; }
          fit=$( { hash_list $B/fit-inputs.txt
                   git ls-tree HEAD -- $(cat $B/fit-modules.txt)
                   echo "$QUARTUS_IMAGE"; } | sha256sum | cut -d' ' -f1)
          sgn=$( { echo "$fit"; hash_list $B/signoff-inputs.txt; } \
                 | sha256sum | cut -d' ' -f1)
          bs=$(  { echo "$sgn"; hash_list $B/bitstream-inputs.txt
                   cat $B/bitstream-values.txt; } | sha256sum | cut -d' ' -f1)
          {
            echo "fit=pocket-fit-$fit"
            echo "signoff=pocket-signoff-$sgn"
            echo "bitstream=pocket-bitstream-$bs"
          } >> "$GITHUB_OUTPUT"
          cat "$GITHUB_OUTPUT"

      # A tag fits cold, so nothing is released on a core this flow did not
      # build itself.
      #
      # After configure, because quartus_fit writes the project file back —
      # restamped, repathed, the template's assignments dropped where ours
      # displaced them — so the database was made against a QSF that differs
      # from the one file(GENERATE) just wrote. This puts the fitter's own
      # back, and nothing reconfigures after here.
      #
      # .ninja_log rides along with every tier because it is the other half of
      # what a restored stamp is worth: ninja will not accept an output it has
      # no record of producing, whatever the date on it says. The tiers overlap
      # so the last one restored carries the fullest record.
      - uses: actions/cache/restore@v4
        id: fit
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        with:
          key: ${{ steps.keys.outputs.fit }}
          path: |
            build/pocket/.ninja_log
            build/pocket/bitstream/db
            build/pocket/bitstream/output_files
            build/pocket/bitstream/core
            build/pocket/bitstream/rp6502.qsf
            build/pocket/bitstream/rp6502.qpf
            build/pocket/bitstream/fit.stamp

      - uses: actions/cache/restore@v4
        id: signoff
        if: ${{ !startsWith(github.ref, 'refs/tags/') && steps.fit.outputs.cache-hit == 'true' }}
        with:
          key: ${{ steps.keys.outputs.signoff }}
          path: |
            build/pocket/.ninja_log
            build/pocket/bitstream/signoff.stamp

      - uses: actions/cache/restore@v4
        id: bitstream
        if: ${{ !startsWith(github.ref, 'refs/tags/') && steps.signoff.outputs.cache-hit == 'true' }}
        with:
          key: ${{ steps.keys.outputs.bitstream }}
          path: |
            build/pocket/.ninja_log
            build/pocket/bitstream/core.bin

      # After the restore, which overwrites .ninja_log: built first, their
      # entries would go with it and they would build again — after the stamps
      # were dated, which is the one order that re-fits. The generated packages
      # are fit inputs, so a regenerated one is a moved source.
      - name: Build the generated assets and the soft CPU firmware
        run: |
          set -euxo pipefail
          cmake --build build/pocket
          cmake --build build/pocket --target sw_bin

      # A cache restores the mtimes it saved, which are older than the checkout
      # that just landed, so a restored stamp reads as stale. Dated in the
      # order the graph builds them and after everything they stand for is
      # already on disk, so none is older than its own inputs.
      - name: Date the restored work to now
        run: |
          set -euxo pipefail
          B=build/pocket/bitstream
          for f in fit.stamp signoff.stamp core.bin; do
            [ -e "$B/$f" ] && touch "$B/$f"
          done
          true

      # Only when a Quartus stage is owed. Deliberate rather than left to the
      # first docker run, so a bad image fails here and not inside a fit.
      - name: Fetch Quartus
        if: ${{ steps.bitstream.outputs.cache-hit != 'true' }}
        run: |
          set -euxo pipefail
          docker pull "$QUARTUS_IMAGE"
          quartus_sta --version

      # The firmware, the fit, the bitstream it goes into and the card tree are
      # one graph, so asking for the card asks for whatever of it is still owed
      # in the order it has to happen.
      - name: Build core package
        run: cmake --build --preset "Card package"
        working-directory: src/rtl

      # Zipped here rather than downstream so the card's own root is the zip's
      # root, and so an empty package fails this job instead of quietly
      # shipping a release with no core in it.
      - name: Package pocket
        run: |
          set -euxo pipefail
          cd build/pocket/package
          zip -r -y "$GITHUB_WORKSPACE/$ARCHIVE" .

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error

      # After the artifact, so a run that produced a core has already said so
      # before spending time on a cache. A tag skips the restores, so it skips
      # the saves too rather than colliding with an entry holding the same
      # bytes under the same name.
      - uses: actions/cache/save@v4
        if: ${{ !startsWith(github.ref, 'refs/tags/') && steps.fit.outputs.cache-hit != 'true' }}
        with:
          key: ${{ steps.keys.outputs.fit }}
          path: |
            build/pocket/.ninja_log
            build/pocket/bitstream/db
            build/pocket/bitstream/output_files
            build/pocket/bitstream/core
            build/pocket/bitstream/rp6502.qsf
            build/pocket/bitstream/rp6502.qpf
            build/pocket/bitstream/fit.stamp

      - uses: actions/cache/save@v4
        if: ${{ !startsWith(github.ref, 'refs/tags/') && steps.signoff.outputs.cache-hit != 'true' }}
        with:
          key: ${{ steps.keys.outputs.signoff }}
          path: |
            build/pocket/.ninja_log
            build/pocket/bitstream/signoff.stamp

      - uses: actions/cache/save@v4
        if: ${{ !startsWith(github.ref, 'refs/tags/') && steps.bitstream.outputs.cache-hit != 'true' }}
        with:
          key: ${{ steps.keys.outputs.bitstream }}
          path: |
            build/pocket/.ninja_log
            build/pocket/bitstream/core.bin

  # ---------------------------------------------------------------------------
  # package: collect the per-target assets each job already built and checksum
  # them. Nothing is repacked here — an archive made anywhere but on its own
  # platform loses what that platform's tool was preserving.
  #
  # The one artifact holding all of them is for developers, who do want every
  # platform at once. It is not what the release page serves: that gets the
  # assets themselves, one per platform, so nobody downloads six to use one.
  # ---------------------------------------------------------------------------
  package:
    needs: [version, pico, linux, macos, itch-io, windows, android, pocket]
    env:
      ARCHIVE: rp6502-${{ needs.version.outputs.version }}-everything.zip
    runs-on: ubuntu-24.04
    steps:
      # skip-decompress, because these artifacts are the assets themselves. The
      # default unpacks anything that looks like an archive, which for us is
      # every one of them.
      - uses: actions/download-artifact@v8
        with:
          path: _artifacts
          skip-decompress: true

      - name: Collect and checksum
        env:
          V: ${{ needs.version.outputs.version }}
        run: |
          set -euxo pipefail
          mkdir dist

          # This list is the order the assets appear on the release page, and
          # checking it here is what makes a missing one a build failure rather
          # than a quiet gap in the release. Every target builds on every run,
          # pocket included — it restores a cached fit rather than skipping —
          # so there is no asset this tolerates being absent.
          assets=()
          for w in pico.zip windows.zip macos.zip \
                   linux-x86_64.tar.gz linux-aarch64.tar.gz \
                   pocket.zip itch.io.zip android.apk; do
            f="rp6502-${V}-${w}"
            src=$(find _artifacts -type f -name "$f" | head -1)
            if [ -z "$src" ]; then
              echo "missing asset: $f" >&2
              exit 1
            fi
            mv "$src" "dist/$f"
            assets+=("$f")
          done

          ( cd dist && sha256sum "${assets[@]}" > SHA256SUMS )
          cat dist/SHA256SUMS

          # Zipped here rather than left to upload-artifact, so this artifact
          # is one archive with the assets at its root, like every other one.
          ( cd dist && zip -r "$GITHUB_WORKSPACE/$ARCHIVE" . )

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ env.ARCHIVE }}
          path: ${{ env.ARCHIVE }}
          archive: false
          if-no-files-found: error
          retention-days: 14

  # ---------------------------------------------------------------------------
  # release: on a v* tag, publish one asset per target as a GitHub Release.
  # ---------------------------------------------------------------------------
  release:
    needs: [version, package]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      # Unpacked here rather than by the download, which would do it silently
      # and only on a tag — the one run nobody gets to try again.
      - uses: actions/download-artifact@v8
        with:
          name: rp6502-${{ needs.version.outputs.version }}-everything.zip
          path: .
          skip-decompress: true

      - name: Unpack the release
        env:
          ARCHIVE: rp6502-${{ needs.version.outputs.version }}-everything.zip
        run: |
          set -euxo pipefail
          unzip -q "$ARCHIVE" -d dist
          ls -l dist

      - name: Compose release title (matches historical "Version X" format)
        id: title
        run: echo "name=Version ${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      # Named one by one rather than globbed: the release page lists assets in
      # upload order, and firmware belongs above the emulators.
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: ${{ steps.title.outputs.name }}
          files: |
            dist/rp6502-${{ needs.version.outputs.version }}-pico.zip
            dist/rp6502-${{ needs.version.outputs.version }}-windows.zip
            dist/rp6502-${{ needs.version.outputs.version }}-macos.zip
            dist/rp6502-${{ needs.version.outputs.version }}-linux-x86_64.tar.gz
            dist/rp6502-${{ needs.version.outputs.version }}-linux-aarch64.tar.gz
            dist/rp6502-${{ needs.version.outputs.version }}-pocket.zip
            dist/rp6502-${{ needs.version.outputs.version }}-itch.io.zip
            dist/rp6502-${{ needs.version.outputs.version }}-android.apk
            dist/SHA256SUMS
          fail_on_unmatched_files: true
          generate_release_notes: true
          body: |
            ## Picocomputer 6502 release

            One download per platform, each with install instructions inside.

            | Asset | |
            |---|---|
            | `rp6502-${{ needs.version.outputs.version }}-pico.zip` | RIA and VGA firmware for the Picocomputer |
            | `rp6502-${{ needs.version.outputs.version }}-windows.zip` | Emulator, Windows x64 (Direct3D 11) |
            | `rp6502-${{ needs.version.outputs.version }}-macos.zip` | Emulator, macOS 11+ Apple silicon |
            | `rp6502-${{ needs.version.outputs.version }}-linux-x86_64.tar.gz` | Emulator, Linux x86_64 (glibc 2.35+) |
            | `rp6502-${{ needs.version.outputs.version }}-linux-aarch64.tar.gz` | Emulator, Linux aarch64 (glibc 2.35+) |
            | `rp6502-${{ needs.version.outputs.version }}-pocket.zip` | Analogue Pocket core, extract to the card root |
            | `rp6502-${{ needs.version.outputs.version }}-itch.io.zip` | Publish a program on itch.io |
            | `rp6502-${{ needs.version.outputs.version }}-android.apk` | Emulator, Android arm64 |

            The desktop builds are unsigned and the APK is signed with a debug
            key, so every OS objects on first launch. Windows: "More info" then
            "Run anyway". macOS: "Open Anyway" under Privacy & Security.
            Android: "More info" then "Install anyway".

            The emulators come with no software. 6502 programs are distributed
            as `.rp6502` files — find them on Discord, which has a forum for
            ROMs, or on itch.io under the RP6502 tag.

            - Discord: https://discord.gg/TC6X8kTr6d
            - itch.io: https://itch.io/games/tag-rp6502
            - Documentation: https://picocomputer.github.io/
